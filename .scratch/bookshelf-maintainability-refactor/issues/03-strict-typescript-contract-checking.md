# 03 — Строгая проверка TypeScript contracts

**Spec:** [Предсказуемая архитектура Bookshelf](../spec.md)

**What to build:** Frontend обрабатывает сгенерированные закрытые контракты исчерпывающе и безопасно обращается к коллекциям, чтобы новый вариант или отсутствующий элемент нельзя было пропустить незаметно.

**Blocked by:** 01 — Воспроизводимые локальные quality gates и toolchains; 02 — Воспроизводимая генерация TypeScript contracts.

**Status:** ready-for-agent

- [ ] Включён `noUncheckedIndexedAccess`, а `exactOptionalPropertyTypes` остаётся выключенным.
- [ ] Все появившиеся ошибки исправлены явной обработкой отсутствующих значений без небезопасных глобальных assertions.
- [ ] Закрытые unions обрабатываются исчерпывающе там, где выбор варианта влияет на поведение.
- [ ] Наблюдаемое поведение frontend не изменилось и защищено существующими тестами.
- [ ] Проходят все frontend-команды из `AGENTS.md`; регенерация bindings не создаёт неожиданного diff.
