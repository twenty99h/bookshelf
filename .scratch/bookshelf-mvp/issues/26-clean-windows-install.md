# 26 — Чистая Windows-установка

**What to build:** Пользователь устанавливает Windows-first сборку Bookshelf на чистую поддерживаемую Windows-машину без заранее установленных Rust и Node.js и запускает приложение со всеми необходимыми локальными runtime-компонентами.

**Blocked by:** 01 — Запускаемая Bookshelf с устойчивым локальным состоянием.

**Status:** ready-for-human

- [ ] Установщик успешно ставит и удаляет приложение на чистой поддерживаемой Windows-системе.
- [x] Для запуска не требуются отдельно установленные Rust, Node.js или инструменты разработчика.
- [ ] Приложение использует только заявленные системные WebView-зависимости и понятно сообщает об их отсутствии.
- [ ] Чистая установка запускается и проходит базовый desktop smoke test.

## Comments

NSIS release job ставит, запускает с урезанным PATH, снимает визуальное доказательство и удаляет приложение; системная зависимость WebView2 и ручной clean-install gate документированы.

Повторная code-review проверка установила, что GitHub-hosted runner уже содержит инструменты разработки и не заменяет чистую Windows VM. Финальные три пункта требуют clean-VM запуска человеком.
